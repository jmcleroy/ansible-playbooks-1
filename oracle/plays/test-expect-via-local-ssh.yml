---

- name: text expect
  hosts: all

  tasks:

    - name: run test file
      expect:
        command: ssh cloud-user@{{ ansible_ssh_host }} /home/cloud-user/test_expect.sh
        responses:
          'your choice': 'abcd113'
      delegate_to: 127.0.0.1
